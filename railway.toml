[build]
builder = "NIXPACKS"
buildCommand = "cd arca-bridge && npm ci --omit=dev"

[deploy]
startCommand = "cd arca-bridge && npm start"
healthcheckPath = "/health/wsfe"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
